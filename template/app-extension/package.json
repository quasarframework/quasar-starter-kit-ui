{
  "name": "quasar-app-extension-{{name}}",
  "version": "0.1.0",
  "description": "{{aeDescription}}",
  "author": "{{author}}",
  "license": "{{license}}",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc"
  },
  "repository": {
    "type": "{{repositoryType}}",
    "url": "{{repositoryURL}}"
  },
  "bugs": "{{bugs}}",
  "homepage": "{{homepage}}",
  "dependencies": {
    "quasar-ui-{{name}}": "^0.1.0"
  },
  "engines": {
    "node": ">= 8.9.0",
    "npm": ">= 5.6.0",
    "yarn": ">= 1.6.0"
  }
}
