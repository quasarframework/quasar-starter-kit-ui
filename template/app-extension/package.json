{
  "name": "quasar-app-extension-{{name}}",
  "version": "0.1.0",
  "description": "{{aeDescription}}",
  "author": "{{author}}",
  "license": "{{license}}",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && cp -r ./src/templates ./dist/templates"
  },
  "repository": {
    "type": "{{repositoryType}}",
    "url": "{{repositoryURL}}"
  },
  "bugs": "{{bugs}}",
  "homepage": "{{homepage}}",
  "dependencies": {
    "quasar-ui-{{name}}": "^0.1.0"
  },
  "devDependencies": {
    "@quasar/app": "^3.0.0-beta.12",
    "@types/node": "^14.14.39",
    "typescript": "^4.2.4"
  },
  "engines": {
    "node": ">= 8.9.0",
    "npm": ">= 5.6.0",
    "yarn": ">= 1.6.0"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
